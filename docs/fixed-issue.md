# Fixed Issue Checklist

## Goal

Estimate app fixes must be completed in priority order. Do not move to the next priority while the current priority has failing tests or unresolved implementation errors.

## P0 - Pricing Trust Boundary

- [x] Prevent AI prompt output from overriding deterministic `priceRange`.
- [x] Prevent AI prompt output from overriding deterministic `breakdown`.
- [x] Preserve deterministic itemized pricing metadata only when generated by the pricing flow.
- [x] Add a regression test where AI returns conflicting price data and the deterministic result still wins.
- [x] Run the targeted estimate generation test file.

## P0 - Empty Specific Interior Scope

- [x] Reject `Interior Painting` + `Specific areas only` submissions when no billable room, handrail, door, window, or skirting quantity exists.
- [x] Keep the validation server-side in `estimateRequestSchema`.
- [x] Add a schema regression test for an empty specific-area interior request.
- [x] Run the targeted request schema test file.

## P1 - Stale Payload Normalization

- [x] Add a submit-time normalizer so hidden or stale form fields cannot affect pricing.
- [x] Clear apartment-only fields when submitting a non-apartment property.
- [x] Clear exterior-only fields when exterior work is not selected.
- [x] Clear interior-only fields when interior work is not selected.
- [x] Add coverage for Apartment to House stale `approxSize` or equivalent payload pollution.

## P1 - Estimate Edit Lifecycle

- [x] Return `estimateId` from estimate creation.
- [x] Split create/update/regenerate responsibilities.
- [x] Add `status`, `revision`, `inputSnapshot`, and pricing metadata to saved estimates.
- [x] Preserve previous generated results through revisions or a versions subcollection.
- [x] Add UI entry points for editing and regenerating an existing estimate.

## P2 - Photo Upload Lifecycle

- [x] Create an estimate draft before uploading photos.
- [x] Store photos under an `estimateId` scoped path.
- [x] Clean uploaded photos when generation fails.
- [x] Add server-side protection against authenticated upload abuse.

## P2 - Rate Limit Rollback

- [x] Separate rate-limit reservation from successful estimate commit.
- [x] Roll back hourly and daily counters when generation or Firestore save fails.
- [x] Keep `lastSubmitAt` consistent with successful submissions only, or model pending reservations explicitly.

## P2 - Display Consistency

- [x] Use one formatter for customer and admin price display.
- [x] Keep GST wording as display formatting instead of mutating stored `priceRange` values.
- [x] Make GST wording consistent across result, PDF, dashboard, stored estimate display, and admin detail.

## P3 - Test Coverage

- [x] Cover AI price override regression.
- [x] Cover empty specific-area interior validation.
- [x] Cover stale form payload normalization.
- [x] Cover photo upload cleanup on submit failure.
- [x] Cover rate-limit rollback on submit failure.
- [x] Cover shared price display formatter behavior.

## Progress Log

- [x] P0 pricing trust boundary completed and tested.
- [x] P0 empty specific interior scope completed and tested.
- [x] P1 stale payload normalization completed and tested.
- [x] Estimate lifecycle create/update/regenerate foundation completed and tested.
- [x] Photo upload lifecycle completed and tested.
- [x] Rate-limit reservation rollback completed and tested.
- [x] Customer/admin price display formatting completed and tested.
- [x] Full TypeScript and Jest verification passed.
